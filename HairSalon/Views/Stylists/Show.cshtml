@{
  Layout = "_Layout";
}

@using HairSalon.Models;
@model List<HairSalon.Models.Stylist>;

<h5>Stylist</h5>
@if (@Model.Count == 0)
{
  <h3>No Stylist have been added yet!</h3>
}

@foreach(Stylist stylist in Model)
{
  <p>@Html.ActionLink($"{stylist.StylistName}", "Details", new { id = stylist.StylistId })</p>
}

@* <p>@Html.ActionLink("Add New Stylist", "Create")<h4> @Html.ActionLink("Search Stylist by Name", "Search")</h4> </p> *@
<p>@Html.ActionLink("Stylists", "Index", "StyLists")</p>
<p>@Html.ActionLink("Home", "Index", "Home")</p>